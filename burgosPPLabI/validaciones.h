/** \brief Confirma si la cadena de caracteres ingresada solo contiene caracteres alfabeticos
 *
 * \param Cadena de caracteres
 * \return Devuelve 1 al terminar correctamente, devuelve 0 si la cadena ingresada no cumple con el criterio
 *
 */
int esSoloLetras(char str[]);

/** \brief Confirma si la cadena de caracteres ingresada es un numero entero
 *
 * \param Cadena de caracteres
 * \param
 * \return Devuelve 1 al terminar correctamente, devuelve 0 si la cadena ingresada no cumple con el criterio
 *
 */
int validarStringInt(char stringInt[]);
